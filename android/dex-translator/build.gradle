plugins {
  id('java-library')
}

description = 'Dex Translator'

dependencies {
  api project(':android:dex-reader-api')
  api project(':android:dex-ir')
  api fileTree(dir: '../libs', include: '*.jar')
}

